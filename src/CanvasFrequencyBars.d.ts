/// <reference types="react" />
interface FrequencyBarsProps {
    width: number;
    height: number;
    color?: string;
    widthMultiplier?: number;
}
export declare const FrequencyBars: ({ width, height, color, widthMultiplier, }: FrequencyBarsProps) => import("react").JSX.Element;
export {};
